# virtual env
.venv/

# datasets (multi-GB — download via the scripts, see README)
data/

# large model checkpoints (regenerate via train.py; or host on Releases/LFS)
checkpoints/*.pth

# generated feature table (regenerate via extract_features.py)
survival_features.csv

# experiment tracking / logs
wandb/
*.log

# python
__pycache__/
*.pyc

# jupyter checkpoints
.ipynb_checkpoints/

# personal notes / files (not for the public repo)
write_up
ACCESS.md
*.docx

# generated artifacts — regenerate via the scripts (keep repo to source only)
eda.html
sample_slice.png
survival_*.png
gradcam_overlays.png
pred_vs_gt_dice_dist.png
survival_features_unet.csv
survival_model.joblib

# superseded / helper scripts (kept locally, not tracked)
load_data.py
build_eda.py

# OS
.DS_Store
